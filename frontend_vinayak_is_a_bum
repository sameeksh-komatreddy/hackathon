<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>BackTrack: Posture and Eye Care</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,300..900&family=Nunito:wght@400;600;700;800&display=swap" rel="stylesheet">
<style>
/* ============ TOKENS ============ */
:root{
  --bg:#FDFCF8;
  --card-bg:#FEFEFA;
  --fg:#2C2C24;
  --primary:#5D7052;
  --primary-fg:#F3F4F1;
  --secondary:#C18C5D;
  --secondary-fg:#FFFFFF;
  --accent:#E6DCCD;
  --accent-fg:#4A4A40;
  --muted:#F0EBE5;
  --muted-fg:#78786C;
  --border:#DED8CF;
  --destructive:#A85448;
  --font-display:'Fraunces', serif;
  --font-body:'Nunito', sans-serif;
  --shadow-soft:0 4px 20px -2px rgba(93,112,82,0.15);
  --shadow-float:0 10px 40px -10px rgba(193,140,93,0.2);
  --shadow-lift:0 20px 40px -10px rgba(93,112,82,0.18);
}
*{box-sizing:border-box;}
html,body{margin:0;padding:0;}
body{
  background:var(--bg);
  color:var(--fg);
  font-family:var(--font-body);
  min-height:100vh;
  overflow-x:hidden;
  position:relative;
}
::selection{background:var(--primary);color:#fff;}
:focus-visible{outline:2px solid var(--primary);outline-offset:3px;border-radius:6px;}
button{font-family:inherit;cursor:pointer;}
input,select{font-family:inherit;}

@media (prefers-reduced-motion: reduce){
  *{animation-duration:.001ms !important; animation-iteration-count:1 !important; transition-duration:.001ms !important;}
}

/* ============ AMBIENT BACKGROUND: drifting blobs and grain ============ */
.ambient-blob{position:fixed;pointer-events:none;z-index:0;filter:blur(70px);opacity:.4;}
.blob-a{
  width:480px;height:480px;background:var(--primary);
  border-radius:60% 40% 30% 70% / 60% 30% 70% 40%;
  top:-160px;left:-140px;
  animation:driftA 28s ease-in-out infinite alternate;
}
.blob-b{
  width:420px;height:420px;background:var(--secondary);
  border-radius:30% 70% 70% 30% / 30% 30% 70% 70%;
  bottom:-180px;right:-120px;opacity:.32;
  animation:driftB 34s ease-in-out infinite alternate;
}
.blob-c{
  width:340px;height:340px;background:var(--accent);
  border-radius:70% 30% 50% 50% / 40% 60% 40% 60%;
  top:38%;right:6%;opacity:.5;
  animation:driftC 24s ease-in-out infinite alternate;
}
@keyframes driftA{0%{transform:translate(0,0) scale(1) rotate(0deg);}100%{transform:translate(40px,60px) scale(1.08) rotate(8deg);}}
@keyframes driftB{0%{transform:translate(0,0) scale(1) rotate(0deg);}100%{transform:translate(-50px,-30px) scale(1.05) rotate(-6deg);}}
@keyframes driftC{0%{transform:translate(0,0) scale(1);}100%{transform:translate(-30px,40px) scale(1.12);}}
.grain{
  position:fixed;inset:0;z-index:1;pointer-events:none;
  background-image:radial-gradient(circle at 1px 1px, rgba(44,44,36,0.05) 1px, transparent 0);
  background-size:3px 3px;
  mix-blend-mode:multiply;
  opacity:.55;
}

/* ============ SCAFFOLD ============ */
#app{position:relative;z-index:2;min-height:100vh;}
.hidden{display:none !important;}

/* ============ LOGIN ============ */
#login-screen{
  min-height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:24px;
}
.login-card{
  width:100%;
  max-width:420px;
  background:var(--card-bg);
  border:1px solid var(--border);
  border-radius:2rem;
  border-top-left-radius:4.5rem;
  padding:46px 40px 34px;
  position:relative;
  overflow:hidden;
  box-shadow:var(--shadow-float);
}
.login-card::before{
  content:'';position:absolute;width:150px;height:150px;
  background:var(--accent);opacity:.55;
  border-radius:60% 40% 30% 70% / 60% 30% 70% 40%;
  top:-55px;right:-55px;z-index:0;
}
.login-card > *{position:relative;z-index:1;}
.brand-mark{display:flex;align-items:center;gap:13px;margin-bottom:28px;}
.spine-logo-wrap{
  width:46px;height:46px;border-radius:50%;flex-shrink:0;
  background:var(--primary);display:flex;align-items:center;justify-content:center;
  box-shadow:var(--shadow-soft);
}
.wordmark{font-family:var(--font-display);font-weight:700;font-size:20px;letter-spacing:.01em;color:var(--fg);}
.wordmark small{display:block;font-family:var(--font-body);font-weight:700;font-size:9.5px;letter-spacing:.16em;color:var(--muted-fg);margin-top:3px;text-transform:uppercase;}
.login-card h1{font-family:var(--font-display);font-size:26px;margin:0 0 8px;font-weight:600;color:var(--fg);}
.login-sub{color:var(--muted-fg);font-size:14px;margin:0 0 26px;line-height:1.55;}
.field{margin-bottom:16px;}
.field label{display:block;font-family:var(--font-body);font-weight:700;font-size:10.5px;letter-spacing:.1em;color:var(--muted-fg);text-transform:uppercase;margin-bottom:8px;}
.field input{
  width:100%;background:rgba(255,255,255,.55);border:1.5px solid var(--border);
  border-radius:999px;padding:12px 20px;color:var(--fg);font-size:14px;
  transition:border-color .2s, box-shadow .2s;
}
.field input:focus{border-color:var(--primary);box-shadow:0 0 0 4px rgba(93,112,82,.15);outline:none;}
.btn-primary{
  width:100%;background:var(--primary);
  border:none;border-radius:999px;padding:13px;color:var(--primary-fg);font-weight:700;font-size:15px;
  margin-top:8px;letter-spacing:.01em;
  box-shadow:var(--shadow-soft);
  transition:transform .2s ease, box-shadow .2s ease;
}
.btn-primary:hover{transform:scale(1.03);box-shadow:0 6px 24px -4px rgba(93,112,82,.3);}
.btn-primary:active{transform:scale(0.97);}
.btn-primary:disabled{opacity:.6;cursor:default;transform:none;}
.login-foot{margin-top:24px;text-align:center;font-size:13px;color:var(--muted-fg);}
.link-btn{background:none;border:none;padding:0;color:var(--secondary);font-size:13px;font-weight:700;font-family:inherit;text-decoration:underline;text-underline-offset:3px;cursor:pointer;}
.link-btn:hover{color:var(--destructive);}
.status-strip{
  display:flex;align-items:center;gap:9px;margin-top:24px;padding-top:18px;
  border-top:1px dashed var(--border);font-family:var(--font-body);font-weight:700;font-size:10px;color:var(--muted-fg);letter-spacing:.06em;text-transform:uppercase;
}
.login-error{
  display:none;margin-top:14px;padding:10px 14px;border-radius:1rem;
  background:rgba(168,84,72,.1);border:1px solid rgba(168,84,72,.3);
  color:var(--destructive);font-size:12.5px;font-weight:700;
}
.login-error.show{display:block;}
.dot-pulse{width:7px;height:7px;border-radius:50%;background:var(--primary);box-shadow:0 0 0 0 rgba(93,112,82,.5);animation:pulseDot 2.4s infinite;flex-shrink:0;}
.dot-pulse.offline{background:var(--destructive);box-shadow:none;animation:none;}
@keyframes pulseDot{
  0%{box-shadow:0 0 0 0 rgba(93,112,82,.45);}
  70%{box-shadow:0 0 0 8px rgba(93,112,82,0);}
  100%{box-shadow:0 0 0 0 rgba(93,112,82,0);}
}

/* ============ APP SHELL ============ */
#app-shell{display:grid;grid-template-columns:230px 1fr;min-height:100vh;}

/* Sidebar */
.sidebar{
  border-right:1px solid var(--border);
  background:rgba(254,254,250,.7);
  backdrop-filter:blur(6px);
  padding:26px 18px;
  display:flex;flex-direction:column;
  position:sticky;top:0;height:100vh;
}
.sidebar .brand-mark{padding:0 6px;margin-bottom:36px;}
.nav-group-label{font-family:var(--font-body);font-weight:800;font-size:10px;letter-spacing:.16em;color:var(--muted-fg);text-transform:uppercase;padding:0 12px;margin-bottom:10px;}
.nav-list{list-style:none;margin:0 0 30px;padding:0;display:flex;flex-direction:column;gap:5px;}
.nav-item{
  display:flex;align-items:center;gap:12px;padding:11px 15px;border-radius:999px;
  color:var(--fg);font-size:14px;font-weight:700;background:none;border:none;width:100%;text-align:left;
  transition:background .2s, color .2s, transform .2s;position:relative;
}
.nav-item svg{flex-shrink:0;opacity:.7;}
.nav-item:hover{background:var(--accent);transform:translateX(2px);}
.nav-item.active{background:var(--primary);color:var(--primary-fg);box-shadow:var(--shadow-soft);}
.nav-item.active svg{opacity:1;stroke:var(--primary-fg);}
.nav-item .tick{margin-left:auto;width:7px;height:7px;border-radius:50%;background:var(--destructive);display:none;}
.nav-item[data-alert="1"] .tick{display:block;}

.sidebar-foot{margin-top:auto;}
.session-mini{
  border:1px solid var(--border);border-radius:1.5rem;border-top-right-radius:2.5rem;
  padding:14px 15px 13px;background:var(--accent);
}
.session-mini .label{font-family:var(--font-body);font-weight:800;font-size:10px;color:var(--accent-fg);letter-spacing:.1em;text-transform:uppercase;margin-bottom:7px;}
.session-mini .value{font-family:var(--font-display);font-size:20px;color:var(--primary);font-weight:700;}
.logout-btn{
  width:100%;margin-top:12px;background:none;border:2px solid var(--secondary);color:var(--secondary);
  border-radius:999px;padding:10px;font-size:13px;font-weight:700;transition:all .2s;
}
.logout-btn:hover{background:var(--secondary);color:var(--secondary-fg);}

/* Main */
.main{padding:30px 38px 60px;max-width:1180px;}
.topbar{display:flex;align-items:center;justify-content:space-between;margin-bottom:30px;gap:20px;flex-wrap:wrap;}
.topbar-title{font-family:var(--font-display);font-size:30px;font-weight:600;letter-spacing:-.01em;color:var(--fg);}
.topbar-sub{color:var(--muted-fg);font-size:13.5px;margin-top:4px;}
.topbar-right{display:flex;align-items:center;gap:16px;}
.clock{
  font-family:var(--font-body);font-weight:700;font-size:12.5px;color:var(--fg);
  border:1.5px solid var(--border);border-radius:999px;padding:8px 16px;display:flex;align-items:center;gap:8px;
  background:rgba(255,255,255,.5);backdrop-filter:blur(4px);
}
.avatar{
  width:38px;height:38px;border-radius:50%;background:var(--secondary);
  display:flex;align-items:center;justify-content:center;font-family:var(--font-display);font-weight:700;font-size:13px;
  color:var(--secondary-fg);box-shadow:var(--shadow-soft);
}

.panel{display:none;}
.panel.active{display:block;animation:fadeUp .4s ease both;}
@keyframes fadeUp{from{opacity:0;transform:translateY(8px);}to{opacity:1;transform:translateY(0);}}

.preview-note{
  display:inline-flex;align-items:center;gap:7px;font-family:var(--font-body);font-weight:700;font-size:11.5px;
  color:var(--secondary);background:rgba(193,140,93,.1);border:1px solid rgba(193,140,93,.3);
  border-radius:999px;padding:6px 13px;margin-bottom:20px;
}

/* Cards / grid framework */
.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:20px;}
.grid-3{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;}
.card{
  background:var(--card-bg);border:1px solid rgba(222,216,207,.6);border-radius:2rem;
  position:relative;overflow:hidden;box-shadow:var(--shadow-soft);
  transition:transform .3s ease, box-shadow .3s ease;
}
.card.r-a{border-top-left-radius:3.5rem;}
.card.r-b{border-bottom-right-radius:3.5rem;}
.card.r-c{border-top-right-radius:2.75rem;border-bottom-left-radius:2.75rem;}
.card:hover{transform:translateY(-3px);box-shadow:var(--shadow-lift);}

/* Metric tiles */
.metric-tile{padding:20px 20px 18px;}
.metric-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;}
.metric-label{font-family:var(--font-body);font-weight:800;font-size:10.5px;letter-spacing:.08em;color:var(--muted-fg);text-transform:uppercase;}
.metric-badge{font-family:var(--font-body);font-weight:800;font-size:10.5px;padding:3px 10px;border-radius:999px;letter-spacing:.03em;}
.metric-badge.good{background:rgba(93,112,82,.12);color:var(--primary);border:1px solid rgba(93,112,82,.3);}
.metric-badge.warn{background:rgba(168,84,72,.12);color:var(--destructive);border:1px solid rgba(168,84,72,.3);}
.metric-badge.neutral{background:var(--muted);color:var(--muted-fg);border:1px solid var(--border);}
.metric-value{font-family:var(--font-display);font-size:34px;font-weight:700;letter-spacing:-.01em;color:var(--fg);}
.metric-value span{font-family:var(--font-body);font-size:13px;color:var(--muted-fg);font-weight:700;margin-left:4px;}
.metric-foot{font-size:12.5px;color:var(--muted-fg);margin-top:8px;}
.metric-foot b{color:var(--primary);font-weight:800;}

.score-toggle{
  display:flex;align-items:center;gap:6px;background:none;border:none;padding:0;
  margin-top:12px;font-family:var(--font-body);font-weight:800;font-size:11px;
  color:var(--secondary);letter-spacing:.03em;
}
.score-toggle:hover{color:var(--destructive);}
.score-toggle svg{transition:transform .25s ease;flex-shrink:0;}
.score-toggle.open svg{transform:rotate(180deg);}
.score-breakdown{max-height:0;overflow:hidden;opacity:0;transition:max-height .28s ease, opacity .22s ease, margin-top .28s ease;}
.score-breakdown.open{max-height:320px;opacity:1;margin-top:12px;}
.breakdown-row{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:7px 0;border-bottom:1px dashed var(--border);}
.breakdown-row:last-child{border-bottom:none;}
.bd-left{flex:1;min-width:0;}
.bd-label{display:block;font-family:var(--font-body);font-weight:700;font-size:11px;color:var(--muted-fg);margin-bottom:5px;}
.breakdown-bar{height:4px;background:var(--muted);border-radius:999px;overflow:hidden;}
.breakdown-bar-fill{height:100%;border-radius:999px;background:linear-gradient(90deg,var(--secondary),var(--destructive));}
.breakdown-row b{flex-shrink:0;font-family:var(--font-display);font-size:14px;font-weight:700;color:var(--destructive);}
.breakdown-empty{font-size:12px;color:var(--muted-fg);padding:4px 0 2px;}

/* Section headers */
.section-head{display:flex;align-items:baseline;justify-content:space-between;margin:34px 0 15px;}
.section-head h2{font-family:var(--font-display);font-size:19px;font-weight:600;margin:0;display:flex;align-items:center;gap:10px;color:var(--fg);}
.section-head h2::before{content:'';width:8px;height:8px;background:var(--secondary);border-radius:60% 40% 30% 70% / 60% 30% 70% 40%;}
.section-head .hint{font-family:var(--font-body);font-weight:700;font-size:10.5px;color:var(--muted-fg);letter-spacing:.05em;text-transform:uppercase;}

/* Chart card */
.chart-card{padding:22px 22px 10px;overflow:visible;}
.chart-top{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:8px;}
.chart-title{font-family:var(--font-display);font-weight:600;font-size:16px;color:var(--fg);}
.chart-current{font-family:var(--font-display);font-size:22px;font-weight:700;color:var(--primary);}
svg.chart{width:100%;height:150px;display:block;overflow:visible;}
.chart-axis-labels{display:flex;justify-content:space-between;font-family:var(--font-body);font-weight:700;font-size:10px;color:var(--muted-fg);margin:4px 0 16px;}
.chart-hit{fill:transparent;cursor:pointer;}
.chart-hover-dot{opacity:0;transition:opacity .12s ease;pointer-events:none;stroke:#fff;stroke-width:2;}
.chart-hover-dot.visible{opacity:1;}
.chart-tooltip{
  position:absolute;pointer-events:none;z-index:6;
  transform:translate(-50%,-125%);
  background:var(--fg);color:var(--bg);
  font-family:var(--font-body);font-size:11.5px;font-weight:800;
  padding:7px 12px;border-radius:.8rem;white-space:nowrap;
  box-shadow:var(--shadow-lift);
}
.chart-tooltip span{display:block;font-weight:700;font-size:10px;letter-spacing:.03em;color:rgba(253,252,248,.6);margin-top:2px;}
.chart-tooltip.hidden{display:none;}

.insight-box{
  margin:14px 0 18px;padding:15px 16px;border-radius:1.5rem;border-top-right-radius:2.25rem;
  background:var(--accent);
  border:1px solid var(--border);
}
.insight-box .tag{font-family:var(--font-body);font-weight:800;font-size:10px;letter-spacing:.08em;color:var(--primary);text-transform:uppercase;display:flex;align-items:center;gap:7px;margin-bottom:7px;}
.insight-box p{margin:0;font-size:13.5px;color:var(--accent-fg);line-height:1.6;}
.chart-empty{padding:30px 4px 24px;font-size:13px;color:var(--muted-fg);line-height:1.6;}

/* Record tab */
.rec-grid{display:grid;grid-template-columns:1fr 1fr;gap:20px;}
.cam-panel{padding:0;overflow:hidden;}
.cam-head{display:flex;align-items:center;justify-content:space-between;padding:14px 18px;border-bottom:1px solid var(--border);gap:10px;}
.cam-name{font-family:var(--font-body);font-weight:800;font-size:12px;letter-spacing:.03em;display:flex;align-items:center;gap:8px;color:var(--fg);flex-shrink:0;}
.cam-status{display:flex;align-items:center;gap:7px;font-family:var(--font-body);font-weight:700;font-size:10.5px;color:var(--primary);}
.cam-status.offline{color:var(--destructive);}
.cam-status .dot{width:7px;height:7px;border-radius:50%;background:var(--primary);animation:pulseDot 2.4s infinite;}
.cam-status.offline .dot{background:var(--destructive);animation:none;}
.recal-btn{
  background:none;border:1.5px solid var(--border);color:var(--muted-fg);
  border-radius:999px;padding:5px 12px;font-size:10.5px;font-weight:700;
  transition:border-color .2s, color .2s;
}
.recal-btn:hover:not(:disabled){border-color:var(--primary);color:var(--primary);}
.recal-btn:disabled{opacity:.6;cursor:default;}
.cam-view{
  height:440px;position:relative;
  background:radial-gradient(ellipse at 50% 35%, var(--muted) 0%, var(--accent) 78%);
  display:flex;align-items:center;justify-content:center;overflow:hidden;
}
.cam-feed-img{width:100%;height:100%;object-fit:cover;display:block;}
.cam-placeholder{
  position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;
  background:radial-gradient(ellipse at 50% 35%, var(--muted) 0%, var(--accent) 78%);
  text-align:center;padding:0 20px;
}
.cam-placeholder span{font-family:var(--font-body);font-weight:700;font-size:12.5px;color:var(--accent-fg);line-height:1.5;}
.cam-placeholder.hidden{display:none;}
.cam-foot{display:flex;justify-content:space-between;padding:11px 18px;border-top:1px solid var(--border);font-family:var(--font-body);font-weight:700;font-size:11px;color:var(--muted-fg);}
.cam-foot b{color:var(--fg);}

.rec-controls{display:flex;align-items:center;gap:18px;margin-top:22px;flex-wrap:wrap;}
.record-btn{
  display:flex;align-items:center;gap:11px;background:var(--primary);
  border:none;color:var(--primary-fg);font-weight:800;font-size:14.5px;padding:14px 26px;border-radius:999px;
  box-shadow:var(--shadow-soft);transition:transform .2s ease, box-shadow .2s ease, background .2s ease;
}
.record-btn:hover{transform:scale(1.03);}
.record-btn:active{transform:scale(.97);}
.record-btn.recording{background:var(--destructive);}
.record-btn:disabled{opacity:.6;cursor:default;transform:none;}
.record-btn .rec-dot{width:9px;height:9px;border-radius:50%;background:#fff;}
.record-btn.recording .rec-dot{animation:pulseDot 1.2s infinite;}
.rec-timer{font-family:var(--font-display);font-weight:600;font-size:15px;color:var(--fg);}
.rec-note{font-size:12.5px;color:var(--muted-fg);margin-top:16px;max-width:520px;line-height:1.6;}

/* Threshold progress bar — fills as the user approaches an alert, and
   flashes / resets the moment a notification actually fires. */
.threshold-panel{margin-top:22px;}
.threshold-top{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:9px;}
.threshold-label{font-family:var(--font-body);font-weight:800;font-size:11px;letter-spacing:.06em;color:var(--muted-fg);text-transform:uppercase;}
.threshold-pct{font-family:var(--font-display);font-size:15px;font-weight:700;color:var(--fg);}
.threshold-track{height:12px;background:var(--muted);border-radius:999px;overflow:hidden;border:1px solid var(--border);}
.threshold-fill{
  height:100%;width:0%;border-radius:999px;
  background:linear-gradient(90deg,var(--primary),var(--secondary));
  transition:width .25s ease, background .25s ease;
}
.threshold-fill.hot{background:linear-gradient(90deg,var(--secondary),var(--destructive));}
.threshold-fill.fired{animation:thresholdFlash .5s ease 2;}
@keyframes thresholdFlash{0%,100%{opacity:1;}50%{opacity:.35;}}
.threshold-hint{font-size:11.5px;color:var(--muted-fg);margin-top:8px;line-height:1.5;}

.live-metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:22px;}
.live-metric{padding:16px 18px;}
.live-metric .lm-label{font-family:var(--font-body);font-weight:800;font-size:10px;color:var(--muted-fg);letter-spacing:.08em;text-transform:uppercase;}
.live-metric .lm-val{font-family:var(--font-display);font-size:24px;font-weight:700;margin-top:7px;color:var(--fg);}
.bar-track{height:5px;background:var(--muted);border-radius:999px;margin-top:10px;overflow:hidden;}
.bar-fill{height:100%;background:linear-gradient(90deg,var(--primary),var(--secondary));border-radius:999px;transition:width .3s ease;}

/* Export tab */
.export-layout{display:grid;grid-template-columns:1.1fr .9fr;gap:22px;align-items:start;}
.check-list{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:11px;}
.check-item{
  display:flex;align-items:center;gap:13px;padding:14px 16px;border:1px solid var(--border);
  border-radius:1.5rem;background:var(--card-bg);cursor:pointer;transition:border-color .2s,background .2s,transform .2s;
}
.check-item:hover{border-color:var(--primary);transform:translateX(2px);}
.check-item input{accent-color:var(--primary);width:18px;height:18px;flex-shrink:0;}
.check-item .ci-text b{display:block;font-size:14px;font-weight:800;color:var(--fg);}
.check-item .ci-text span{display:block;font-size:12px;color:var(--muted-fg);margin-top:2px;}
.range-row{display:flex;gap:12px;margin:18px 0 22px;}
.range-row .field{flex:1;margin:0;}
.pdf-btn{
  display:flex;align-items:center;justify-content:center;gap:10px;width:100%;
  background:var(--primary);border:none;color:var(--primary-fg);
  font-weight:800;font-size:14.5px;padding:14px;border-radius:999px;box-shadow:var(--shadow-soft);
  transition:transform .2s ease, opacity .2s ease;
}
.pdf-btn:disabled{opacity:.35;box-shadow:none;cursor:not-allowed;}
.pdf-btn:not(:disabled):hover{transform:scale(1.02);}
.report-preview{padding:20px;}
.report-sheet{
  background:var(--muted);border:1px solid var(--border);border-radius:1.25rem;padding:22px 20px;
  font-family:var(--font-body);
}
.report-sheet .rs-head{display:flex;justify-content:space-between;align-items:flex-start;border-bottom:1px solid var(--border);padding-bottom:11px;margin-bottom:13px;}
.report-sheet .rs-head b{font-family:var(--font-display);font-size:15px;color:var(--fg);}
.report-sheet .rs-head span{font-size:10px;color:var(--muted-fg);font-weight:700;}
.rs-row{display:flex;justify-content:space-between;font-size:12px;color:var(--muted-fg);padding:6px 0;border-bottom:1px dashed var(--border);font-weight:600;}
.rs-row b{color:var(--fg);font-weight:800;}
.rs-block{height:54px;margin:13px 0;border-radius:12px;background:repeating-linear-gradient(135deg, rgba(93,112,82,.1) 0 6px, rgba(93,112,82,.03) 6px 12px);border:1px solid var(--border);}

/* Settings tab */
.settings-grid{display:grid;grid-template-columns:1fr 1fr;gap:20px;}
.settings-card{padding:22px;}
.settings-card h3{font-family:var(--font-display);font-size:16px;margin:0 0 5px;color:var(--fg);font-weight:600;}
.settings-card .sc-sub{font-size:12.5px;color:var(--muted-fg);margin:0 0 20px;}
.slider-row{margin-bottom:22px;}
.slider-row .sr-top{display:flex;justify-content:space-between;font-size:13px;margin-bottom:10px;color:var(--fg);font-weight:700;}
.slider-row .sr-top b{font-family:var(--font-display);color:var(--primary);font-weight:700;}
input[type=range]{
  -webkit-appearance:none;width:100%;height:5px;border-radius:999px;background:var(--muted);outline:none;
}
input[type=range]::-webkit-slider-thumb{
  -webkit-appearance:none;width:18px;height:18px;border-radius:50%;background:var(--primary);
  border:3px solid var(--card-bg);box-shadow:0 0 0 2px var(--primary), var(--shadow-soft);cursor:pointer;margin-top:0;
}
.select-row{margin-bottom:18px;}
.select-row label{display:block;font-family:var(--font-body);font-weight:800;font-size:10.5px;color:var(--muted-fg);letter-spacing:.06em;text-transform:uppercase;margin-bottom:8px;}
.select-row select{
  width:100%;background:rgba(255,255,255,.55);border:1.5px solid var(--border);border-radius:999px;padding:11px 18px;
  color:var(--fg);font-size:14px;font-weight:600;
}
.theme-options{display:flex;gap:12px;}
.theme-opt{
  flex:1;border:2px solid var(--border);border-radius:1.25rem;padding:13px;cursor:pointer;text-align:center;transition:border-color .2s,transform .2s;
}
.theme-opt:hover{transform:translateY(-2px);}
.theme-opt.selected{border-color:var(--primary);background:rgba(93,112,82,.06);}
.theme-opt .swatch{height:28px;border-radius:999px;margin-bottom:9px;}
.theme-opt .swatch.paper{background:linear-gradient(90deg,#FDFCF8,#E6DCCD,#C18C5D);}
.theme-opt .swatch.dusk{background:linear-gradient(90deg,#2C2C24,#5D7052,#A85448);}
.theme-opt span{font-size:12.5px;font-weight:700;color:var(--fg);}
.save-btn{
  margin-top:8px;background:var(--secondary);color:var(--secondary-fg);border:none;border-radius:999px;padding:12px 24px;
  font-weight:800;font-size:14px;box-shadow:var(--shadow-float);transition:transform .2s ease;
}
.save-btn:hover{transform:scale(1.03);}

.danger-card{border-color:rgba(168,84,72,.35);}
.danger-btn{
  background:none;border:2px solid var(--destructive);color:var(--destructive);
  border-radius:999px;padding:12px 24px;font-weight:800;font-size:14px;transition:all .2s;
}
.danger-btn:hover{background:var(--destructive);color:#fff;}
.danger-btn:disabled{opacity:.6;cursor:default;}

/* Toast */
#toast{
  position:fixed;bottom:26px;right:26px;z-index:50;
  background:var(--card-bg);border:1.5px solid rgba(168,84,72,.45);border-radius:1.5rem;border-top-left-radius:2.25rem;
  padding:15px 17px;max-width:320px;display:flex;gap:13px;align-items:flex-start;
  box-shadow:var(--shadow-float);
  transform:translateY(24px);opacity:0;pointer-events:none;transition:transform .35s ease, opacity .35s ease;
}
#toast.show{transform:translateY(0);opacity:1;pointer-events:auto;}
#toast .t-icon{width:32px;height:32px;border-radius:60% 40% 30% 70% / 60% 30% 70% 40%;background:rgba(168,84,72,.15);display:flex;align-items:center;justify-content:center;flex-shrink:0;}
#toast .t-text b{display:block;font-family:var(--font-display);font-size:14px;margin-bottom:3px;color:var(--fg);font-weight:700;}
#toast .t-text span{font-size:12.5px;color:var(--muted-fg);line-height:1.5;}
#toast .t-close{background:none;border:none;color:var(--muted-fg);margin-left:auto;font-size:16px;line-height:1;padding:0;}

/* Responsive */
@media(max-width:980px){
  #app-shell{grid-template-columns:1fr;}
  .sidebar{position:static;height:auto;flex-direction:row;align-items:center;padding:16px 18px;overflow-x:auto;}
  .sidebar .brand-mark{margin-bottom:0;margin-right:16px;}
  .nav-group-label{display:none;}
  .nav-list{flex-direction:row;margin-bottom:0;}
  .sidebar-foot{display:none;}
  .main{padding:24px 20px 50px;}
  .grid-2,.grid-3,.rec-grid,.export-layout,.settings-grid,.live-metrics{grid-template-columns:1fr;}
  .cam-view{height:320px;}
}
</style>
</head>
<body>

<div class="ambient-blob blob-a"></div>
<div class="ambient-blob blob-b"></div>
<div class="ambient-blob blob-c"></div>
<div class="grain"></div>

<div id="app">

  <!-- ======================= LOGIN ======================= -->
  <div id="login-screen">
    <div class="login-card">
      <div class="brand-mark">
        <div class="spine-logo-wrap">
          <svg width="22" height="26" viewBox="0 0 30 34" fill="none">
            <circle cx="15" cy="4" r="3.4" stroke="#F3F4F1" stroke-width="1.8"/>
            <path d="M15 8 L13 13 L16 18 L12 23 L15 29 L14 33" stroke="#F3F4F1" stroke-width="1.8" stroke-linecap="round"/>
            <circle cx="13" cy="13" r="1.6" fill="#F3F4F1"/>
            <circle cx="16" cy="18" r="1.6" fill="#F3F4F1"/>
            <circle cx="12" cy="23" r="1.6" fill="#F3F4F1"/>
          </svg>
        </div>
        <div class="wordmark">BackTrack<small>Posture and Eye Care</small></div>
      </div>
      <h1 id="loginHeading">Sign in</h1>
      <p class="login-sub" id="loginSub">Track posture and eye strain with care, session by session.</p>
      <form id="loginForm" novalidate>
        <div class="field hidden" id="nameFieldWrap">
          <label for="name">Full name</label>
          <input id="name" type="text" placeholder="Jordan Reyes">
        </div>
        <div class="field">
          <label for="username">Username</label>
          <input id="username" type="text" placeholder="jordanreyes" autocomplete="username">
        </div>
        <div class="field">
          <label for="pass">Password</label>
          <input id="pass" type="password" placeholder="Enter your password" autocomplete="current-password">
        </div>
        <button type="submit" class="btn-primary" id="loginSubmitBtn">Sign in</button>
        <div class="login-error" id="loginError"></div>
      </form>
      <p class="login-foot">New to BackTrack? <button type="button" id="toggleSignup" class="link-btn">Create an account</button></p>
      <div class="status-strip"><span class="dot-pulse" id="loginBackendDot"></span> <span id="loginBackendText">Checking backend connection&hellip;</span></div>
    </div>
  </div>

  <!-- ======================= APP SHELL ======================= -->
  <div id="app-shell" class="hidden">

    <!-- Sidebar -->
    <aside class="sidebar">
      <div class="brand-mark">
        <div class="spine-logo-wrap" style="width:38px;height:38px;">
          <svg width="19" height="22" viewBox="0 0 30 34" fill="none">
            <circle cx="15" cy="4" r="3.4" stroke="#F3F4F1" stroke-width="1.8"/>
            <path d="M15 8 L13 13 L16 18 L12 23 L15 29 L14 33" stroke="#F3F4F1" stroke-width="1.8" stroke-linecap="round"/>
            <circle cx="13" cy="13" r="1.6" fill="#F3F4F1"/>
            <circle cx="16" cy="18" r="1.6" fill="#F3F4F1"/>
            <circle cx="12" cy="23" r="1.6" fill="#F3F4F1"/>
          </svg>
        </div>
        <div class="wordmark" style="font-size:17px;">BackTrack</div>
      </div>

      <div class="nav-group-label">Workspace</div>
      <ul class="nav-list" id="navList">
        <li><button class="nav-item active" data-tab="history">
          <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M3 3v18h18"/><path d="M7 15l4-6 4 3 5-8"/></svg>
          History</button></li>
        <li><button class="nav-item" data-tab="record">
          <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="12" cy="12" r="8"/><circle cx="12" cy="12" r="3" fill="currentColor" stroke="none"/></svg>
          Record<span class="tick"></span></button></li>
        <li><button class="nav-item" data-tab="export">
          <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M12 3v12"/><path d="M7 10l5 5 5-5"/><path d="M4 19h16"/></svg>
          Export</button></li>
        <li><button class="nav-item" data-tab="settings">
          <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.7 1.7 0 00.3 1.9l.1.1a2 2 0 11-2.8 2.8l-.1-.1a1.7 1.7 0 00-1.9-.3 1.7 1.7 0 00-1 1.6V21a2 2 0 11-4 0v-.1a1.7 1.7 0 00-1-1.6 1.7 1.7 0 00-1.9.3l-.1.1a2 2 0 112.8-2.8l.1-.1a1.7 1.7 0 00.3-1.9 1.7 1.7 0 00-1.6-1H3a2 2 0 110-4h.1a1.7 1.7 0 001.6-1 1.7 1.7 0 00-.3-1.9l-.1-.1a2 2 0 112.8 2.8l-.1.1a1.7 1.7 0 00-.3 1.9V9a1.7 1.7 0 001.6 1H21a2 2 0 110 4h-.1a1.7 1.7 0 00-1.6 1z"/></svg>
          Settings</button></li>
      </ul>

      <div class="sidebar-foot">
        <div class="session-mini">
          <div class="label">Latest session score</div>
          <div class="value"><span id="sidebarScoreValue">--</span> <span style="font-size:12px;color:var(--muted-fg);font-weight:600;font-family:var(--font-body);">out of 100</span></div>
        </div>
        <button class="logout-btn" id="logoutBtn">Sign out</button>
      </div>
    </aside>

    <!-- Main -->
    <main class="main">

      <div class="topbar">
        <div>
          <div class="topbar-title" id="pageTitle">History</div>
          <div class="topbar-sub" id="pageSub">Review sessions and long-term trends.</div>
        </div>
        <div class="topbar-right">
          <div class="clock"><span class="dot-pulse"></span><span id="clockText">--:--:--</span></div>
          <div class="avatar" id="avatarInitials">JR</div>
        </div>
      </div>

      <!-- ============ HISTORY PANEL ============ -->
      <section class="panel active" id="panel-history">
        <div class="preview-note hidden" id="historyStatusNote">Connecting to the BackTrack backend&hellip;</div>

        <div class="grid-3">
          <div class="card r-a metric-tile">
            <div class="metric-head"><span class="metric-label">Average posture score</span><span class="metric-badge neutral" id="tilePostureBadge">No data</span></div>
            <div class="metric-value" id="tilePostureValue">--<span>out of 100</span></div>
            <div class="metric-foot" id="tilePostureFoot">No sessions recorded yet</div>
            <button class="score-toggle" type="button" id="postureBreakdownToggle" aria-expanded="false">
              <span>See score breakdown</span>
              <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M6 9l6 6 6-6"/></svg>
            </button>
            <div class="score-breakdown" id="postureBreakdown"></div>
          </div>
          <div class="card r-c metric-tile">
            <div class="metric-head"><span class="metric-label">Eye strain index</span><span class="metric-badge neutral" id="tileEyeBadge">No data</span></div>
            <div class="metric-value" id="tileEyeValue">--<span>out of 10</span></div>
            <div class="metric-foot" id="tileEyeFoot">No sessions recorded yet</div>
            <button class="score-toggle" type="button" id="eyeBreakdownToggle" aria-expanded="false">
              <span>See score breakdown</span>
              <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M6 9l6 6 6-6"/></svg>
            </button>
            <div class="score-breakdown" id="eyeBreakdown"></div>
          </div>
          <div class="card r-b metric-tile">
            <div class="metric-head"><span class="metric-label">Sessions logged</span><span class="metric-badge neutral" id="tileSessionsBadge">No data</span></div>
            <div class="metric-value" id="tileSessionsValue">0<span>logged</span></div>
            <div class="metric-foot" id="tileSessionsFoot">No sessions recorded yet</div>
          </div>
        </div>

        <div class="section-head"><h2>Posture score trend</h2><span class="hint">Most recent sessions</span></div>
        <div class="card r-a chart-card" id="postureChartCard">
          <div class="chart-top"><span class="chart-title">Posture Score</span><span class="chart-current" id="postureCurrentVal">--</span></div>
          <div class="chart-empty" id="postureChartEmpty">Record at least two sessions from the Record tab to see a trend line here.</div>
          <div id="postureChartBody" class="hidden">
            <svg class="chart" viewBox="0 0 600 150" preserveAspectRatio="none">
              <defs>
                <linearGradient id="gradPosture" x1="0" y1="0" x2="0" y2="1">
                  <stop offset="0%" stop-color="#5D7052" stop-opacity="0.32"/>
                  <stop offset="100%" stop-color="#5D7052" stop-opacity="0"/>
                </linearGradient>
              </defs>
              <g stroke="#DED8CF" stroke-width="1">
                <line x1="0" y1="30" x2="600" y2="30"/><line x1="0" y1="75" x2="600" y2="75"/><line x1="0" y1="120" x2="600" y2="120"/>
              </g>
              <polyline id="postureLine" points="" fill="none" stroke="#5D7052" stroke-width="2.75" stroke-linecap="round" stroke-linejoin="round"/>
              <polygon id="postureFill" points="" fill="url(#gradPosture)"/>
              <circle id="postureHoverDot" class="chart-hover-dot" cx="0" cy="0" r="6" fill="#5D7052"/>
              <circle id="postureDot" cx="0" cy="0" r="5" fill="#5D7052"/>
              <g id="postureHitPoints"></g>
            </svg>
            <div class="chart-tooltip hidden" id="postureTooltip"></div>
            <div class="chart-axis-labels" id="postureAxisLabels"></div>
            <div class="insight-box" id="postureInsightBox">
              <div class="tag"><svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2l2.4 6.6L21 11l-6.6 2.4L12 20l-2.4-6.6L3 11l6.6-2.4z"/></svg> Session insight</div>
              <p id="postureInsightText"></p>
            </div>
          </div>
        </div>

        <div class="section-head"><h2>Eye strain trend</h2><span class="hint">Most recent sessions</span></div>
        <div class="card r-c chart-card" id="eyeChartCard">
          <div class="chart-top"><span class="chart-title">Eye Strain Index</span><span class="chart-current" id="eyeCurrentVal" style="color:var(--destructive);">--</span></div>
          <div class="chart-empty" id="eyeChartEmpty">Record at least two sessions from the Record tab to see a trend line here.</div>
          <div id="eyeChartBody" class="hidden">
            <svg class="chart" viewBox="0 0 600 150" preserveAspectRatio="none">
              <defs>
                <linearGradient id="gradStrain" x1="0" y1="0" x2="0" y2="1">
                  <stop offset="0%" stop-color="#C18C5D" stop-opacity="0.3"/>
                  <stop offset="100%" stop-color="#C18C5D" stop-opacity="0"/>
                </linearGradient>
              </defs>
              <g stroke="#DED8CF" stroke-width="1">
                <line x1="0" y1="30" x2="600" y2="30"/><line x1="0" y1="75" x2="600" y2="75"/><line x1="0" y1="120" x2="600" y2="120"/>
              </g>
              <polyline id="eyeLine" points="" fill="none" stroke="#C18C5D" stroke-width="2.75" stroke-linecap="round" stroke-linejoin="round"/>
              <polygon id="eyeFill" points="" fill="url(#gradStrain)"/>
              <circle id="eyeHoverDot" class="chart-hover-dot" cx="0" cy="0" r="6" fill="#C18C5D"/>
              <circle id="eyeDot" cx="0" cy="0" r="5" fill="#C18C5D"/>
              <g id="eyeHitPoints"></g>
            </svg>
            <div class="chart-tooltip hidden" id="eyeTooltip"></div>
            <div class="chart-axis-labels" id="eyeAxisLabels"></div>
            <div class="insight-box" id="eyeInsightBox">
              <div class="tag"><svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2l2.4 6.6L21 11l-6.6 2.4L12 20l-2.4-6.6L3 11l6.6-2.4z"/></svg> Session insight</div>
              <p id="eyeInsightText"></p>
            </div>
          </div>
        </div>

      </section>

      <!-- ============ RECORD PANEL ============ -->
      <section class="panel" id="panel-record">
        <div class="preview-note" id="backendStatusNote">Press Start recording to connect to the BackTrack backend at 127.0.0.1:5050.</div>

        <div class="rec-grid">
          <div class="card r-a cam-panel">
            <div class="cam-head">
              <span class="cam-name"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="6" width="14" height="12" rx="2"/><path d="M16 10l6-4v12l-6-4"/></svg> Front camera</span>
              <span class="cam-status offline" id="frontCamStatus"><span class="dot"></span>Not connected</span>
              <button class="recal-btn" data-cam="front">Recalibrate</button>
            </div>
            <div class="cam-view" id="frontCamView">
              <img class="cam-feed-img hidden" id="frontFeedImg" alt="Front camera feed">
              <div class="cam-placeholder" id="frontPlaceholder">
                <span class="dot-pulse offline"></span>
                <span>Front camera preview appears here once you press Start recording.</span>
              </div>
            </div>
            <div class="cam-foot"><span>Head pitch <b id="frontPitchVal">0.0 degrees</b></span><span>Head roll <b id="frontRollVal">0.0 degrees</b></span></div>
          </div>

          <div class="card r-c cam-panel">
            <div class="cam-head">
              <span class="cam-name"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="7" y="2" width="10" height="20" rx="2"/><line x1="11" y1="18" x2="13" y2="18"/></svg> Side camera</span>
              <span class="cam-status offline" id="sideCamStatus"><span class="dot"></span>Not connected</span>
              <button class="recal-btn" data-cam="side">Recalibrate</button>
            </div>
            <div class="cam-view" id="sideCamView">
              <img class="cam-feed-img hidden" id="sideFeedImg" alt="Side camera feed">
              <div class="cam-placeholder" id="sidePlaceholder">
                <span class="dot-pulse offline"></span>
                <span>Side camera preview appears here once you press Start recording.</span>
              </div>
            </div>
            <div class="cam-foot"><span>Neck angle <b id="sideNeckVal">0.0 degrees</b></span><span>Torso lean <b id="sideTorsoVal">0.0 degrees</b></span></div>
          </div>
        </div>

        <div class="rec-controls">
          <button class="record-btn" id="recordBtn"><span class="rec-dot"></span><span id="recordBtnText">Start recording</span></button>
          <span class="rec-timer" id="recTimer">00:00:00</span>
        </div>
        <p class="rec-note">Notifications will alert you when posture or eye strain thresholds are crossed, both as pop-ups here and as system notifications on your device, for as long as a session is recording — even if another window is focused.</p>

        <div class="card r-b threshold-panel" style="padding:20px 22px;">
          <div class="threshold-top">
            <span class="threshold-label">Closest threshold</span>
            <span class="threshold-pct" id="thresholdPct">0%</span>
          </div>
          <div class="threshold-track"><div class="threshold-fill" id="thresholdFill"></div></div>
          <p class="threshold-hint" id="thresholdHint">This fills up as you hold a posture or eye-strain issue. When it's full, a notification fires — on screen here and on your device.</p>
        </div>

        <div class="section-head"><h2>Live readout</h2><span class="hint">Updating from the backend stream</span></div>
        <div class="live-metrics">
          <div class="card r-a live-metric">
            <div class="lm-label">Neck angle</div><div class="lm-val" id="lmNeck">0.0 degrees</div>
            <div class="bar-track"><div class="bar-fill" id="lmNeckBar" style="width:0%;"></div></div>
          </div>
          <div class="card r-b live-metric">
            <div class="lm-label">Blink rate</div><div class="lm-val" id="lmBlink">0<span style="font-size:13px;color:var(--muted-fg);font-family:var(--font-body);"> per minute</span></div>
            <div class="bar-track"><div class="bar-fill" id="lmBlinkBar" style="width:0%;background:linear-gradient(90deg,var(--destructive),var(--secondary));"></div></div>
          </div>
          <div class="card r-c live-metric">
            <div class="lm-label">Elapsed</div><div class="lm-val" id="lmElapsed">00:00</div>
            <div class="bar-track"><div class="bar-fill" id="lmElapsedBar" style="width:0%;"></div></div>
          </div>
        </div>
      </section>

      <!-- ============ EXPORT PANEL ============ -->
      <section class="panel" id="panel-export">
        <div class="preview-note" id="exportPreviewNote">Pulling your session history from the backend…</div>
        <div class="export-layout">
          <div>
            <div class="section-head" style="margin-top:0;"><h2>Include in report</h2></div>
            <ul class="check-list">
              <li class="check-item"><input type="checkbox" checked id="chkPosture"><div class="ci-text"><b>Posture data</b><span>Session scores, angle readings, and drift patterns</span></div></li>
              <li class="check-item"><input type="checkbox" checked id="chkEye"><div class="ci-text"><b>Eye strain data</b><span>Blink rate and strain index history</span></div></li>
              <li class="check-item"><input type="checkbox" id="chkTrend"><div class="ci-text"><b>Trend summary</b><span>Session over session changes and automated insights</span></div></li>
              <li class="check-item"><input type="checkbox" id="chkSessions"><div class="ci-text"><b>Session log</b><span>Full list of individual sessions with duration and averages</span></div></li>
            </ul>

            <div class="range-row">
              <div class="field"><label>From</label><input type="date" id="exportFrom"></div>
              <div class="field"><label>To</label><input type="date" id="exportTo"></div>
            </div>

            <button class="pdf-btn" id="pdfBtn">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3v12"/><path d="M7 10l5 5 5-5"/><path d="M4 19h16"/></svg>
              Generate PDF report
            </button>
            <p class="rec-note">Built to hand to a physical therapist or doctor: plain figures, no jargon, one section per category.</p>
          </div>

          <div class="card r-b report-preview">
            <div class="report-sheet">
              <div class="rs-head"><b>BackTrack Report</b><span id="rsDate">--</span></div>
              <div class="rs-row"><span>Patient</span><b id="rsPatient">BackTrack User</b></div>
              <div class="rs-row"><span>Range</span><b id="rsRange">--</b></div>
              <div class="rs-row"><span>Average posture score</span><b id="rsPosture">-- out of 100</b></div>
              <div class="rs-row"><span>Average eye strain index</span><b id="rsEye">-- out of 10</b></div>
              <div class="rs-block"></div>
              <div class="rs-row"><span>Sessions included</span><b id="rsSessions">0</b></div>
              <div class="rs-row" style="border-bottom:none;"><span>Format</span><b id="rsFormat">PDF</b></div>
            </div>
          </div>
        </div>
      </section>

      <!-- ============ SETTINGS PANEL ============ -->
      <section class="panel" id="panel-settings">
        <div class="preview-note" id="settingsStatusNote">Loading current thresholds from the backend…</div>
        <div class="settings-grid">
          <div class="card r-a settings-card">
            <h3>Posture sensitivity</h3>
            <p class="sc-sub">Set how much forward lean, tilt, or shoulder rise triggers an alert.</p>
            <div class="slider-row">
              <div class="sr-top"><span>Neck angle threshold</span><b id="valNeckThresh">20 degrees</b></div>
              <input type="range" min="5" max="60" step="1" value="20" id="sliderNeckThresh">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Torso lean threshold</span><b id="valTorsoThresh">15 degrees</b></div>
              <input type="range" min="5" max="60" step="1" value="15" id="sliderTorsoThresh">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Head pitch threshold</span><b id="valPitchThresh">25 degrees</b></div>
              <input type="range" min="5" max="60" step="1" value="25" id="sliderPitchThresh">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Head roll threshold</span><b id="valRollThresh">25 degrees</b></div>
              <input type="range" min="5" max="60" step="1" value="25" id="sliderRollThresh">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Shoulder shrug sensitivity</span><b id="valShrugRatio">90%</b></div>
              <input type="range" min="50" max="99" step="1" value="90" id="sliderShrugRatio">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Shrug sustain before alert</span><b id="valShrugDur">10 seconds</b></div>
              <input type="range" min="1" max="120" step="1" value="10" id="sliderShrugDur">
            </div>
            <div class="slider-row" style="margin-bottom:4px;">
              <div class="sr-top"><span>Hunching sustain before alert</span><b id="valSideDur">30 seconds</b></div>
              <input type="range" min="1" max="180" step="1" value="30" id="sliderSideDur">
            </div>
          </div>

          <div class="card r-c settings-card">
            <h3>Eye strain sensitivity</h3>
            <p class="sc-sub">Set the eye narrowing and blink rate that count as strain.</p>
            <div class="slider-row">
              <div class="sr-top"><span>Eye narrowing threshold (EAR)</span><b id="valEarThresh">0.22</b></div>
              <input type="range" min="5" max="40" step="1" value="22" id="sliderEarThresh">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Blink detection threshold (EAR)</span><b id="valBlinkThresh">0.18</b></div>
              <input type="range" min="5" max="40" step="1" value="18" id="sliderBlinkThresh">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Minimum blink rate</span><b id="valLowBlinkRate">15 per minute</b></div>
              <input type="range" min="2" max="40" step="1" value="15" id="sliderLowBlinkRate">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Blink rate calculation window</span><b id="valMinBlinkWin">10 seconds</b></div>
              <input type="range" min="2" max="60" step="1" value="10" id="sliderMinBlinkWin">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Eye narrowing sustain before alert</span><b id="valEarDur">10 seconds</b></div>
              <input type="range" min="1" max="120" step="1" value="10" id="sliderEarDur">
            </div>
            <div class="slider-row">
              <div class="sr-top"><span>Low blink sustain before alert</span><b id="valBlinkDur">10 seconds</b></div>
              <input type="range" min="1" max="120" step="1" value="10" id="sliderBlinkDur">
            </div>
            <div class="slider-row" style="margin-bottom:4px;">
              <div class="sr-top"><span>Head tilt sustain before alert</span><b id="valHeadDur">10 seconds</b></div>
              <input type="range" min="1" max="120" step="1" value="10" id="sliderHeadDur">
            </div>
          </div>

          <div class="card r-b settings-card">
            <h3>Notifications</h3>
            <p class="sc-sub">Choose how often BackTrack can repeat the same alert.</p>
            <div class="slider-row" style="margin-bottom:4px;">
              <div class="sr-top"><span>Notification cooldown</span><b id="valCooldown">60 seconds</b></div>
              <input type="range" min="5" max="600" step="5" id="sliderCooldown" value="60">
            </div>
            <p class="sc-sub" style="margin:14px 0 0;">Reminder frequency and quiet hours below are local display preferences and aren't yet read by the backend.</p>
            <div class="select-row" style="margin-top:10px;">
              <label>Reminder frequency</label>
              <select><option>Every 10 minutes</option><option selected>Every 20 minutes</option><option>Every 30 minutes</option><option>Only on threshold breach</option></select>
            </div>
            <div class="select-row" style="margin-bottom:0;">
              <label>Quiet hours</label>
              <select><option>None</option><option selected>7:00 PM to 8:00 AM</option><option>Custom range</option></select>
            </div>
          </div>

          <div class="card r-a settings-card">
            <h3>Appearance</h3>
            <p class="sc-sub">Pick the display theme for the whole app.</p>
            <div class="theme-options">
              <div class="theme-opt selected"><div class="swatch paper"></div><span>Rice Paper</span></div>
              <div class="theme-opt"><div class="swatch dusk"></div><span>Loam Dusk</span></div>
            </div>
          </div>

          <div class="card r-c settings-card danger-card">
            <h3>Danger zone</h3>
            <p class="sc-sub">Permanently erase every session and reset all thresholds for this account. This can't be undone.</p>
            <button class="danger-btn" id="deleteDataBtn">Delete all my data</button>
          </div>
        </div>

        <div style="display:flex;align-items:center;gap:16px;flex-wrap:wrap;">
          <button class="save-btn" id="saveSettingsBtn">Save preferences</button>
          <button type="button" class="link-btn" id="resetSettingsBtn">Reset thresholds to defaults</button>
        </div>
      </section>

    </main>
  </div>
</div>

<!-- Toast -->
<div id="toast">
  <div class="t-icon">
    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="#A85448" stroke-width="2"><path d="M12 9v4"/><path d="M12 17h.01"/><circle cx="12" cy="12" r="9"/></svg>
  </div>
  <div class="t-text"><b id="toastTitle">Posture threshold reached</b><span id="toastBody">You've been leaning forward for 45 seconds. Try straightening up.</span></div>
  <button class="t-close" id="toastClose">Close</button>
</div>

<script>
/* ============ BACKEND CONNECTION ============ */
const BACKEND_URL = 'http://127.0.0.1:5050';
let eventSource = null;
let streamConnected = false;
let lastAlertsKey = '';

/* ============ AUTH STATE ============ */
let authToken = localStorage.getItem('backtrack_token') || null;
let currentUser = null; // {id, username, name}

function authHeaders(extra){
  const h = Object.assign({}, extra || {});
  if(authToken) h['Authorization'] = 'Bearer ' + authToken;
  return h;
}

/* Desktop notification permission — asked once the user is signed in, so
   BackTrack can also raise a native browser/OS notification (in addition
   to the ones the Python backend sends directly to the OS). */
function ensureNotificationPermission(){
  if('Notification' in window && Notification.permission === 'default'){
    Notification.requestPermission().catch(()=>{});
  }
}
function fireBrowserNotification(title, body){
  if('Notification' in window && Notification.permission === 'granted'){
    try{ new Notification(title, { body }); } catch(e){}
  }
}

function connectStream(){
  if(eventSource) return;
  eventSource = new EventSource(BACKEND_URL + '/stream');

  eventSource.onopen = function(){
    streamConnected = true;
  };
  eventSource.onerror = function(){
    streamConnected = false;
    const note = document.getElementById('backendStatusNote');
    if(note && recording) note.textContent = 'Lost connection to the BackTrack backend. Reconnecting…';
    setCamStatus('frontCamStatus', false);
    setCamStatus('sideCamStatus', false);
  };
  eventSource.onmessage = function(e){
    let data;
    try{ data = JSON.parse(e.data); } catch(err){ return; }
    applyStreamData(data);
  };
}
function disconnectStream(){
  if(eventSource){ eventSource.close(); eventSource = null; }
  streamConnected = false;
  setThresholdBar(0);
}

function setCamStatus(id, online){
  const el = document.getElementById(id);
  if(!el) return;
  el.classList.toggle('offline', !online);
  el.innerHTML = '<span class="dot"></span>' + (online ? 'Connected' : 'Not connected');
}

/* ============ THRESHOLD PROGRESS BAR ============ */
const thresholdFill = document.getElementById('thresholdFill');
const thresholdPct = document.getElementById('thresholdPct');
const thresholdHint = document.getElementById('thresholdHint');
let lastProgress = 0;

function setThresholdBar(progress){
  const pct = Math.round(Math.max(0, Math.min(1, progress)) * 100);
  thresholdFill.style.width = pct + '%';
  thresholdPct.textContent = pct + '%';
  thresholdFill.classList.toggle('hot', pct >= 70);
  if(pct >= 100){
    thresholdHint.textContent = "Threshold reached — a notification just fired, on screen and on your device.";
  } else if(pct >= 70){
    thresholdHint.textContent = "Getting close — adjust now to avoid a notification.";
  } else {
    thresholdHint.textContent = "This fills up as you hold a posture or eye-strain issue. When it's full, a notification fires — on screen here and on your device.";
  }
}

function applyStreamData(data){
  setCamStatus('frontCamStatus', !!data.calibrated_front);
  setCamStatus('sideCamStatus', !!data.calibrated_side);

  document.getElementById('frontPitchVal').textContent = (Math.abs(data.pitch || 0)).toFixed(1) + ' degrees';
  document.getElementById('frontRollVal').textContent = (Math.abs(data.roll || 0)).toFixed(1) + ' degrees';
  document.getElementById('sideNeckVal').textContent = (data.neck_angle || 0).toFixed(1) + ' degrees';
  document.getElementById('sideTorsoVal').textContent = (data.torso_angle || 0).toFixed(1) + ' degrees';

  const neckVal = data.neck_angle || 0;
  document.getElementById('lmNeck').textContent = neckVal.toFixed(1) + ' degrees';
  document.getElementById('lmNeckBar').style.width = Math.min(100, (neckVal / 40) * 100) + '%';

  const blinkVal = data.blink_rate || 0;
  const lmBlinkEl = document.getElementById('lmBlink');
  lmBlinkEl.innerHTML = Math.round(blinkVal) + '<span style="font-size:13px;color:var(--muted-fg);font-family:var(--font-body);"> per minute</span>';
  document.getElementById('lmBlinkBar').style.width = Math.min(100, (blinkVal / 30) * 100) + '%';

  const progress = typeof data.progress === 'number' ? data.progress : 0;
  setThresholdBar(progress);
  if(lastProgress < 1 && progress >= 1){
    thresholdFill.classList.remove('fired');
    void thresholdFill.offsetWidth; // restart animation
    thresholdFill.classList.add('fired');
  }
  lastProgress = progress;

  const alerts = Array.isArray(data.alerts) ? data.alerts : [];
  const alertsKey = alerts.join('|');
  if(alerts.length && alertsKey !== lastAlertsKey){
    showToast('Alert from BackTrack', alerts[0]);
    fireBrowserNotification('BackTrack alert', alerts[0]);
  }
  lastAlertsKey = alertsKey;
}

/* ============ CAMERA FEEDS ============ */
function connectCamFeed(imgId, placeholderId, path){
  const img = document.getElementById(imgId);
  const placeholder = document.getElementById(placeholderId);
  img.onload = function(){
    img.classList.remove('hidden');
    placeholder.classList.add('hidden');
  };
  img.onerror = function(){
    img.classList.add('hidden');
    placeholder.classList.remove('hidden');
  };
  img.src = BACKEND_URL + path + '?t=' + Date.now();
}
function disconnectCamFeed(imgId, placeholderId){
  const img = document.getElementById(imgId);
  img.onload = null;
  img.onerror = null;
  img.removeAttribute('src');
  img.classList.add('hidden');
  document.getElementById(placeholderId).classList.remove('hidden');
}

/* ============ RECALIBRATE ============ */
document.querySelectorAll('.recal-btn').forEach(function(btn){
  btn.addEventListener('click', async function(){
    const cam = this.dataset.cam;
    const endpoint = cam === 'front' ? '/recalibrate_front' : '/recalibrate_side';
    const original = this.textContent;
    this.textContent = 'Recalibrating';
    this.disabled = true;
    try{
      await fetch(BACKEND_URL + endpoint, { method: 'POST' });
    } catch(err){
      console.warn('The recalibration request may still have reached the backend even though the response could not be read here.', err);
    }
    setTimeout(() => { this.textContent = original; this.disabled = false; }, 1500);
  });
});

/* ============ LOGIN / SIGNUP ============ */
function initialsFor(name){
  const parts = (name || '').trim().split(/\s+/).filter(Boolean);
  if(parts.length === 0) return '?';
  if(parts.length === 1) return parts[0].slice(0,2).toUpperCase();
  return (parts[0][0] + parts[parts.length-1][0]).toUpperCase();
}

async function enterApp(){
  document.getElementById('login-screen').classList.add('hidden');
  document.getElementById('app-shell').classList.remove('hidden');
  document.getElementById('avatarInitials').textContent = initialsFor(currentUser ? currentUser.name : '');
  ensureNotificationPermission();
  loadHistory();
  loadSettingsFromBackend();
}

async function checkBackendOnline(){
  const dot = document.getElementById('loginBackendDot');
  const text = document.getElementById('loginBackendText');
  try{
    const resp = await fetch(BACKEND_URL + '/', { method: 'GET' });
    if(!resp.ok) throw new Error('bad status');
    dot.classList.remove('offline');
    text.textContent = 'Backend connected — ready to sign in';
  } catch(err){
    dot.classList.add('offline');
    text.textContent = "Couldn't reach the BackTrack backend at 127.0.0.1:5050";
  }
}
checkBackendOnline();

function showLoginError(msg){
  const el = document.getElementById('loginError');
  el.textContent = msg;
  el.classList.add('show');
}
function clearLoginError(){
  document.getElementById('loginError').classList.remove('show');
}

async function tryResumeSession(){
  if(!authToken) return;
  try{
    const resp = await fetch(BACKEND_URL + '/auth/me', { headers: authHeaders() });
    if(!resp.ok) throw new Error('bad status');
    const data = await resp.json();
    currentUser = data.user;
    enterApp();
  } catch(err){
    authToken = null;
    localStorage.removeItem('backtrack_token');
  }
}
tryResumeSession();

document.getElementById('loginForm').addEventListener('submit', async function(e){
  e.preventDefault();
  clearLoginError();
  const username = document.getElementById('username').value.trim();
  const password = document.getElementById('pass').value;
  const name = document.getElementById('name').value.trim();
  const btn = document.getElementById('loginSubmitBtn');

  if(!username || !password){
    showLoginError('Enter a username and password.');
    return;
  }

  btn.disabled = true;
  const original = btn.textContent;
  btn.textContent = isSignup ? 'Creating account…' : 'Signing in…';

  try{
    const endpoint = isSignup ? '/auth/signup' : '/auth/login';
    const body = isSignup ? { username, password, name } : { username, password };
    const resp = await fetch(BACKEND_URL + endpoint, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    });
    const data = await resp.json();
    if(!resp.ok || !data.ok){
      throw new Error(data.error || 'Something went wrong.');
    }
    authToken = data.token;
    currentUser = data.user;
    localStorage.setItem('backtrack_token', authToken);
    enterApp();
  } catch(err){
    showLoginError(err.message || "Couldn't reach the BackTrack backend at 127.0.0.1:5050.");
  } finally {
    btn.disabled = false;
    btn.textContent = original;
  }
});

document.getElementById('logoutBtn').addEventListener('click', async function(){
  document.getElementById('app-shell').classList.add('hidden');
  document.getElementById('login-screen').classList.remove('hidden');
  disconnectStream();
  try{ await fetch(BACKEND_URL + '/auth/logout', { method: 'POST', headers: authHeaders() }); } catch(err){}
  authToken = null;
  currentUser = null;
  localStorage.removeItem('backtrack_token');
  document.getElementById('pass').value = '';
});

let isSignup = false;
const toggleSignupBtn = document.getElementById('toggleSignup');
const loginHeading = document.getElementById('loginHeading');
const loginSub = document.getElementById('loginSub');
const loginSubmitBtn = document.getElementById('loginSubmitBtn');
const nameFieldWrap = document.getElementById('nameFieldWrap');

toggleSignupBtn.addEventListener('click', function(){
  isSignup = !isSignup;
  clearLoginError();
  if(isSignup){
    loginHeading.textContent = 'Create your account';
    loginSub.textContent = 'Set up BackTrack to start tracking posture and eye strain.';
    loginSubmitBtn.textContent = 'Create account';
    nameFieldWrap.classList.remove('hidden');
    toggleSignupBtn.textContent = 'Sign in instead';
    toggleSignupBtn.parentElement.firstChild.textContent = 'Already have an account? ';
  } else {
    loginHeading.textContent = 'Sign in';
    loginSub.textContent = 'Track posture and eye strain with care, session by session.';
    loginSubmitBtn.textContent = 'Sign in';
    nameFieldWrap.classList.add('hidden');
    toggleSignupBtn.textContent = 'Create an account';
    toggleSignupBtn.parentElement.firstChild.textContent = 'New to BackTrack? ';
  }
});

/* ============ TAB NAVIGATION ============ */
const titles = {
  history: ["History", "Review sessions and long-term trends."],
  record: ["Record", "Capture a new session from both camera angles."],
  export: ["Export", "Turn your history into a report you can share."],
  settings: ["Settings", "Tune thresholds, reminders, and appearance."]
};
document.querySelectorAll('.nav-item').forEach(btn => {
  btn.addEventListener('click', function(){
    document.querySelectorAll('.nav-item').forEach(b => b.classList.remove('active'));
    this.classList.add('active');
    const tab = this.dataset.tab;
    document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
    document.getElementById('panel-'+tab).classList.add('active');
    document.getElementById('pageTitle').textContent = titles[tab][0];
    document.getElementById('pageSub').textContent = titles[tab][1];

    if(tab === 'history'){
      loadHistory();
    }
    if(tab === 'export'){
      refreshExportPreview();
    }
    if(tab === 'settings'){
      loadSettingsFromBackend();
    }
  });
});

/* ============ CLOCK ============ */
function updateClock(){
  const d = new Date();
  document.getElementById('clockText').textContent = d.toLocaleTimeString('en-US', {hour12:false});
}
updateClock(); setInterval(updateClock, 1000);

/* ============ RECORD BUTTON, TIMER, AND BACKEND SESSION ============ */
let recording = false, seconds = 0, timerInterval = null;
const recordBtn = document.getElementById('recordBtn');
const recordBtnText = document.getElementById('recordBtnText');
const recTimer = document.getElementById('recTimer');
const lmElapsed = document.getElementById('lmElapsed');
const lmElapsedBar = document.getElementById('lmElapsedBar');
const backendStatusNote = document.getElementById('backendStatusNote');

function fmt(s){
  const hh = String(Math.floor(s/3600)).padStart(2,'0');
  const mm = String(Math.floor((s%3600)/60)).padStart(2,'0');
  const ss = String(s%60).padStart(2,'0');
  return hh+':'+mm+':'+ss;
}
function fmtShort(s){
  const mm = String(Math.floor(s/60)).padStart(2,'0');
  const ss = String(s%60).padStart(2,'0');
  return mm+':'+ss;
}

async function startBackendSession(){
  try{
    const resp = await fetch(BACKEND_URL + '/session/start', { method: 'POST', headers: authHeaders() });
    if(!resp.ok) throw new Error('bad status');
    backendStatusNote.textContent = 'Connected to the BackTrack backend. Recording in progress.';
    return true;
  } catch(err){
    backendStatusNote.textContent = "Couldn't reach the BackTrack backend at 127.0.0.1:5050. Start it and try again.";
    return false;
  }
}

async function stopBackendSession(){
  try{
    const resp = await fetch(BACKEND_URL + '/session/stop', { method: 'POST', headers: authHeaders() });
    if(!resp.ok) throw new Error('bad status');
    const data = await resp.json();
    backendStatusNote.textContent = 'Session saved. Press Start recording to begin a new session.';
    return data.session || null;
  } catch(err){
    backendStatusNote.textContent = 'Recording stopped, but the session may not have been saved — check the backend connection.';
    return null;
  }
}

recordBtn.addEventListener('click', async function(){
  if(!recording){
    recordBtn.disabled = true;
    const started = await startBackendSession();
    recordBtn.disabled = false;
    if(!started) return;

    recording = true;
    connectStream();
    connectCamFeed('frontFeedImg', 'frontPlaceholder', '/feed/front');
    connectCamFeed('sideFeedImg', 'sidePlaceholder', '/feed/side');

    recordBtn.classList.add('recording');
    recordBtnText.textContent = 'Stop recording';
    seconds = 0;
    recTimer.textContent = fmt(0);
    lmElapsed.textContent = fmtShort(0);
    lmElapsedBar.style.width = '0%';
    setThresholdBar(0);
    timerInterval = setInterval(() => {
      seconds++;
      recTimer.textContent = fmt(seconds);
      lmElapsed.textContent = fmtShort(seconds);
      lmElapsedBar.style.width = Math.min(100, (seconds/1800)*100) + '%';
    }, 1000);

  } else {
    recordBtn.disabled = true;
    const summary = await stopBackendSession();
    recordBtn.disabled = false;

    recording = false;
    recordBtn.classList.remove('recording');
    recordBtnText.textContent = 'Start recording';
    clearInterval(timerInterval);

    disconnectStream();
    disconnectCamFeed('frontFeedImg', 'frontPlaceholder');
    disconnectCamFeed('sideFeedImg', 'sidePlaceholder');
    setCamStatus('frontCamStatus', false);
    setCamStatus('sideCamStatus', false);
    setThresholdBar(0);

    if(summary){
      const scoreText = summary.posture_score != null ? Math.round(summary.posture_score) : '--';
      showToast('Session saved', 'Posture score ' + scoreText + ' out of 100 — added to your history.');
    }
    loadHistory();
  }
});

/* ============ TOAST ============ */
const toast = document.getElementById('toast');
function showToast(title, body){
  document.getElementById('toastTitle').textContent = title;
  document.getElementById('toastBody').textContent = body;
  toast.classList.add('show');
  clearTimeout(toast._hideTimer);
  toast._hideTimer = setTimeout(() => toast.classList.remove('show'), 6000);
}
document.getElementById('toastClose').addEventListener('click', () => toast.classList.remove('show'));

/* ============ HISTORY: LOAD FROM BACKEND ============ */
function average(arr){ return arr.length ? arr.reduce((a,b)=>a+b,0)/arr.length : null; }

function averageBreakdown(sessions, key, fields){
  const rows = sessions.map(s => s[key]).filter(b => b != null);
  const out = {};
  fields.forEach(f => {
    const vals = rows.map(b => b[f]).filter(v => v != null);
    out[f] = vals.length ? average(vals) : null;
  });
  return out;
}

function renderBreakdown(containerId, rows){
  const el = document.getElementById(containerId);
  const usable = rows.filter(r => r.val != null);
  if(usable.length === 0){
    el.innerHTML = '<div class="breakdown-empty">Not enough data yet to break this down.</div>';
    return;
  }
  el.innerHTML = usable.map(r => {
    const pct = Math.max(0, Math.min(100, (r.val / r.max) * 100));
    return '<div class="breakdown-row"><div class="bd-left"><span class="bd-label">' + r.label + '</span>'
      + '<div class="breakdown-bar"><div class="breakdown-bar-fill" style="width:' + pct.toFixed(0) + '%;"></div></div>'
      + '</div><b>-' + r.val.toFixed(1) + '</b></div>';
  }).join('');
}

function bindBreakdownToggle(toggleId, panelId){
  const btn = document.getElementById(toggleId);
  const panel = document.getElementById(panelId);
  btn.addEventListener('click', () => {
    const open = panel.classList.toggle('open');
    btn.classList.toggle('open', open);
    btn.setAttribute('aria-expanded', open ? 'true' : 'false');
    btn.querySelector('span').textContent = open ? 'Hide score breakdown' : 'See score breakdown';
  });
}
bindBreakdownToggle('postureBreakdownToggle', 'postureBreakdown');
bindBreakdownToggle('eyeBreakdownToggle', 'eyeBreakdown');

function fmtDateShort(iso){
  const d = new Date(iso);
  if(isNaN(d.getTime())) return '';
  return d.toLocaleDateString('en-US', {month:'short', day:'numeric'});
}

function pickLabelSubset(dates){
  if(dates.length<=4) return dates;
  const idxs = [0, Math.round((dates.length-1)/3), Math.round((dates.length-1)*2/3), dates.length-1];
  return [...new Set(idxs)].map(i=>dates[i]);
}

function buildAxisLabels(containerId, labels){
  const el = document.getElementById(containerId);
  el.innerHTML = '';
  labels.forEach(txt => {
    const span = document.createElement('span');
    span.textContent = txt;
    el.appendChild(span);
  });
}

function setBadge(id, cls, text){
  const el = document.getElementById(id);
  el.className = 'metric-badge ' + cls;
  el.textContent = text;
}

function drawTrendLine(prefix, values, maxValue, labels, fmtValue){
  const n = values.length;
  const line = document.getElementById(prefix+'Line');
  const fill = document.getElementById(prefix+'Fill');
  const dot = document.getElementById(prefix+'Dot');
  const pts = values.map((v,i) => {
    const x = n===1 ? 0 : (i/(n-1))*600;
    const y = 150 - Math.max(0, Math.min(1, v/maxValue))*130 - 10;
    return [x,y];
  });
  const ptsStr = pts.map(p => p[0].toFixed(1)+','+p[1].toFixed(1)).join(' ');
  line.setAttribute('points', ptsStr);
  fill.setAttribute('points', ptsStr + ' 600,150 0,150');
  const last = pts[pts.length-1];
  dot.setAttribute('cx', last[0].toFixed(1));
  dot.setAttribute('cy', last[1].toFixed(1));

  const hitGroup = document.getElementById(prefix+'HitPoints');
  const hoverDot = document.getElementById(prefix+'HoverDot');
  const tooltip = document.getElementById(prefix+'Tooltip');
  const card = document.getElementById(prefix+'ChartCard');
  if(!hitGroup || !tooltip || !card) return;

  hitGroup.innerHTML = '';
  const svgNS = 'http://www.w3.org/2000/svg';

  function positionTooltip(evt){
    const rect = card.getBoundingClientRect();
    tooltip.style.left = (evt.clientX - rect.left) + 'px';
    tooltip.style.top = (evt.clientY - rect.top) + 'px';
  }

  pts.forEach((p, i) => {
    const c = document.createElementNS(svgNS, 'circle');
    c.setAttribute('cx', p[0].toFixed(1));
    c.setAttribute('cy', p[1].toFixed(1));
    c.setAttribute('r', 12);
    c.setAttribute('class', 'chart-hit');

    c.addEventListener('mouseenter', evt => {
      tooltip.innerHTML = '<b>' + fmtValue(values[i]) + '</b><span>' + (labels[i]||'') + '</span>';
      tooltip.classList.remove('hidden');
      if(hoverDot){
        hoverDot.setAttribute('cx', p[0].toFixed(1));
        hoverDot.setAttribute('cy', p[1].toFixed(1));
        hoverDot.classList.add('visible');
      }
      positionTooltip(evt);
    });
    c.addEventListener('mousemove', positionTooltip);
    c.addEventListener('mouseleave', () => {
      tooltip.classList.add('hidden');
      if(hoverDot) hoverDot.classList.remove('visible');
    });

    hitGroup.appendChild(c);
  });
}

function buildPostureInsight(trend){
  const vals = trend.map(s=>s.posture_score);
  const necks = trend.map(s=>s.avg_neck_angle).filter(v=>v!=null);
  const avgNeck = average(necks);
  const first = vals[0], last = vals[vals.length-1];
  const direction = last>first+2 ? 'trending upward' : last<first-2 ? 'trending downward' : 'holding fairly steady';
  let text = `Over the last ${vals.length} sessions your posture score is ${direction}, moving from ${Math.round(first)} to ${Math.round(last)}.`;
  if(avgNeck!=null) text += ` Average neck angle across these sessions was ${avgNeck.toFixed(1)} degrees.`;
  return text;
}

function buildEyeInsight(trend){
  const vals = trend.map(s=>s.eye_strain_index);
  const blinks = trend.map(s=>s.avg_blink_rate).filter(v=>v!=null);
  const avgBlink = average(blinks);
  const first = vals[0], last = vals[vals.length-1];
  const direction = last>first+0.5 ? 'climbing' : last<first-0.5 ? 'easing' : 'holding steady';
  let text = `Your eye strain index is ${direction} across the last ${vals.length} sessions, from ${first.toFixed(1)} to ${last.toFixed(1)}.`;
  if(avgBlink!=null) text += ` Average blink rate over that span was ${avgBlink.toFixed(1)} per minute.`;
  return text;
}

function setEmptyHistoryState(){
  document.getElementById('tilePostureValue').innerHTML = '--<span>out of 100</span>';
  document.getElementById('tileEyeValue').innerHTML = '--<span>out of 10</span>';
  document.getElementById('tileSessionsValue').innerHTML = '0<span>logged</span>';
  document.getElementById('tileSessionsFoot').textContent = 'No sessions recorded yet';
  document.getElementById('tilePostureFoot').textContent = 'No sessions recorded yet';
  document.getElementById('tileEyeFoot').textContent = 'No sessions recorded yet';
  setBadge('tilePostureBadge','neutral','No data');
  setBadge('tileEyeBadge','neutral','No data');
  setBadge('tileSessionsBadge','neutral','No data');
  document.getElementById('postureCurrentVal').textContent = '--';
  document.getElementById('eyeCurrentVal').textContent = '--';
  document.getElementById('postureChartEmpty').classList.remove('hidden');
  document.getElementById('postureChartBody').classList.add('hidden');
  document.getElementById('eyeChartEmpty').classList.remove('hidden');
  document.getElementById('eyeChartBody').classList.add('hidden');
  renderBreakdown('postureBreakdown', []);
  renderBreakdown('eyeBreakdown', []);
}

async function fetchSessions(){
  const resp = await fetch(BACKEND_URL + '/sessions?limit=30', { headers: authHeaders() });
  if(!resp.ok) throw new Error('bad status');
  const data = await resp.json();
  return Array.isArray(data.sessions) ? data.sessions : [];
}

async function loadHistory(){
  const note = document.getElementById('historyStatusNote');
  let sessions;
  try{
    sessions = await fetchSessions();
  } catch(err){
    note.textContent = "Couldn't reach the BackTrack backend at 127.0.0.1:5050. Start it and reload this tab to see your history.";
    note.classList.remove('hidden');
    setEmptyHistoryState();
    return;
  }

  const scored = sessions.filter(s => s.posture_score != null && s.eye_strain_index != null);

  if(scored.length === 0){
    note.textContent = 'No completed sessions yet. Head to the Record tab and press Start recording to log your first session.';
    note.classList.remove('hidden');
    setEmptyHistoryState();
    return;
  }

  note.classList.add('hidden');

  const avgPosture = average(scored.map(s=>s.posture_score));
  const avgEye = average(scored.map(s=>s.eye_strain_index));
  const avgMinutes = average(sessions.map(s=>s.duration_seconds/60));

  document.getElementById('tilePostureValue').innerHTML = Math.round(avgPosture) + '<span>out of 100</span>';
  document.getElementById('tileEyeValue').innerHTML = avgEye.toFixed(1) + '<span>out of 10</span>';
  document.getElementById('tileSessionsValue').innerHTML = sessions.length + '<span>logged</span>';
  document.getElementById('tileSessionsFoot').innerHTML = 'Average length: <b>' + Math.round(avgMinutes) + ' minute' + (Math.round(avgMinutes)===1?'':'s') + '</b>';

  const postureBreakdown = averageBreakdown(scored, 'posture_breakdown', ['neck','torso','pitch','roll','shrug']);
  renderBreakdown('postureBreakdown', [
    {label:'Neck angle',     val: postureBreakdown.neck,  max:40},
    {label:'Torso lean',     val: postureBreakdown.torso, max:30},
    {label:'Head pitch',     val: postureBreakdown.pitch, max:15},
    {label:'Head roll',      val: postureBreakdown.roll,  max:15},
    {label:'Shoulder shrug', val: postureBreakdown.shrug, max:10},
  ]);

  const eyeBreakdown = averageBreakdown(scored, 'eye_breakdown', ['blink','ear']);
  renderBreakdown('eyeBreakdown', [
    {label:'Low blink rate', val: eyeBreakdown.blink, max:6},
    {label:'Eye narrowing',  val: eyeBreakdown.ear,   max:4},
  ]);

  if(scored.length >= 2){
    const last = scored[scored.length-1], prev = scored[scored.length-2];
    const pDelta = Math.round(last.posture_score - prev.posture_score);
    const eDelta = Math.round((last.eye_strain_index - prev.eye_strain_index)*10)/10;
    setBadge('tilePostureBadge', pDelta>=0 ? 'good':'warn', (pDelta>=0?'Up ':'Down ')+Math.abs(pDelta)+' vs last session');
    setBadge('tileEyeBadge', eDelta<=0 ? 'good':'warn', (eDelta<=0?'Down ':'Up ')+Math.abs(eDelta)+' vs last session');
  } else {
    setBadge('tilePostureBadge','neutral','First session');
    setBadge('tileEyeBadge','neutral','First session');
  }
  setBadge('tileSessionsBadge','neutral', sessions.length===1 ? '1 session' : sessions.length+' sessions');

  let streak = 0;
  for(let i=scored.length-1;i>=0;i--){ if(scored[i].posture_score>80) streak++; else break; }
  document.getElementById('tilePostureFoot').innerHTML = streak>0
    ? 'Current streak: <b>'+streak+' session'+(streak===1?'':'s')+'</b> above 80'
    : 'No active streak above 80 right now';

  const lastBlink = scored[scored.length-1].avg_blink_rate;
  document.getElementById('tileEyeFoot').innerHTML = (lastBlink!=null)
    ? 'Latest avg blink rate: <b>'+lastBlink+' per minute</b>'
    : 'No blink data recorded yet';

  const trend = scored.slice(-14);
  const dates = trend.map(s => fmtDateShort(s.end_time));

  if(trend.length >= 2){
    document.getElementById('postureChartEmpty').classList.add('hidden');
    document.getElementById('postureChartBody').classList.remove('hidden');
    document.getElementById('eyeChartEmpty').classList.add('hidden');
    document.getElementById('eyeChartBody').classList.remove('hidden');

    buildAxisLabels('postureAxisLabels', pickLabelSubset(dates));
    buildAxisLabels('eyeAxisLabels', pickLabelSubset(dates));

    const postureVals = trend.map(s=>s.posture_score);
    const eyeVals = trend.map(s=>s.eye_strain_index);

    drawTrendLine('posture', postureVals, 100, dates, v => Math.round(v) + ' / 100');
    drawTrendLine('eye', eyeVals, 10, dates, v => v.toFixed(1) + ' / 10');

    const lastP = postureVals[postureVals.length-1], firstP = postureVals[0];
    const pDiff = Math.abs(Math.round(lastP-firstP));
    document.getElementById('postureCurrentVal').innerHTML = Math.round(lastP) +
      ' <span style="font-size:13px;color:'+(lastP>=firstP?'var(--primary)':'var(--destructive)')+';">'+(lastP>=firstP?'up ':'down ')+pDiff+'</span>';

    const lastE = eyeVals[eyeVals.length-1], firstE = eyeVals[0];
    const eDiff = Math.abs(lastE-firstE).toFixed(1);
    document.getElementById('eyeCurrentVal').innerHTML = lastE.toFixed(1) +
      ' <span style="font-size:13px;color:'+(lastE<=firstE?'var(--primary)':'var(--destructive)')+';">'+(lastE<=firstE?'down ':'up ')+eDiff+'</span>';

    document.getElementById('postureInsightText').textContent = buildPostureInsight(trend);
    document.getElementById('eyeInsightText').textContent = buildEyeInsight(trend);
  } else {
    document.getElementById('postureChartEmpty').classList.remove('hidden');
    document.getElementById('postureChartBody').classList.add('hidden');
    document.getElementById('eyeChartEmpty').classList.remove('hidden');
    document.getElementById('eyeChartBody').classList.add('hidden');
    document.getElementById('postureCurrentVal').textContent = Math.round(trend[0].posture_score);
    document.getElementById('eyeCurrentVal').textContent = trend[0].eye_strain_index.toFixed(1);
  }

  const sidebarScore = document.getElementById('sidebarScoreValue');
  if(sidebarScore) sidebarScore.textContent = Math.round(scored[scored.length-1].posture_score);
}

/* ============ EXPORT: CHECKBOXES, DATE RANGE, LIVE PREVIEW ============ */
const checks = ['chkPosture','chkEye','chkTrend','chkSessions'].map(id => document.getElementById(id));
const pdfBtn = document.getElementById('pdfBtn');
const exportFromInput = document.getElementById('exportFrom');
const exportToInput = document.getElementById('exportTo');

function fmtDateLong(iso){
  const d = new Date(iso + 'T00:00:00');
  if(isNaN(d.getTime())) return iso;
  return d.toLocaleDateString('en-US', {month:'long', day:'numeric', year:'numeric'});
}

function refreshPdfBtn(){
  const anyChecked = checks.some(c => c.checked);
  pdfBtn.disabled = !anyChecked;
}
checks.forEach(c => c.addEventListener('change', refreshPdfBtn));
refreshPdfBtn();

function defaultExportRange(){
  const to = new Date();
  const from = new Date();
  from.setDate(from.getDate() - 30);
  const iso = d => d.toISOString().slice(0,10);
  if(!exportFromInput.value) exportFromInput.value = iso(from);
  if(!exportToInput.value) exportToInput.value = iso(to);
}
defaultExportRange();

async function refreshExportPreview(){
  const note = document.getElementById('exportPreviewNote');
  document.getElementById('rsPatient').textContent = currentUser ? currentUser.name : 'BackTrack User';
  document.getElementById('rsDate').textContent = new Date().toLocaleDateString('en-US', {month:'long', day:'numeric', year:'numeric'});

  let allSessions;
  try{
    const resp = await fetch(BACKEND_URL + '/sessions?limit=0', { headers: authHeaders() });
    if(!resp.ok) throw new Error('bad status');
    const data = await resp.json();
    allSessions = Array.isArray(data.sessions) ? data.sessions : [];
  } catch(err){
    note.textContent = "Couldn't reach the BackTrack backend — connect it to preview and generate reports.";
    note.classList.remove('hidden');
    return;
  }

  const from = exportFromInput.value, to = exportToInput.value;
  const inRange = allSessions.filter(s => {
    const d = (s.start_time || '').slice(0,10);
    if(!d) return false;
    if(from && d < from) return false;
    if(to && d > to) return false;
    return true;
  });
  const scored = inRange.filter(s => s.posture_score != null && s.eye_strain_index != null);

  document.getElementById('rsRange').textContent = (from ? fmtDateLong(from) : 'earliest') + ' to ' + (to ? fmtDateLong(to) : 'latest');
  document.getElementById('rsPosture').textContent = scored.length ? Math.round(average(scored.map(s=>s.posture_score))) + ' out of 100' : '--';
  document.getElementById('rsEye').textContent = scored.length ? average(scored.map(s=>s.eye_strain_index)).toFixed(1) + ' out of 10' : '--';
  document.getElementById('rsSessions').textContent = inRange.length;
  document.getElementById('rsFormat').textContent = 'PDF';

  if(inRange.length === 0){
    note.textContent = 'No sessions fall in this date range yet — widen the range or record a session first.';
    note.classList.remove('hidden');
  } else {
    note.textContent = inRange.length + ' session' + (inRange.length===1?'':'s') + ' found in this range, ready to export.';
    note.classList.remove('hidden');
  }
}
exportFromInput.addEventListener('change', refreshExportPreview);
exportToInput.addEventListener('change', refreshExportPreview);

pdfBtn.addEventListener('click', async function(){
  if(pdfBtn.disabled) return;
  const original = pdfBtn.innerHTML;
  pdfBtn.disabled = true;
  pdfBtn.innerHTML = 'Generating report…';
  try{
    const sections = {
      posture: document.getElementById('chkPosture').checked,
      eye: document.getElementById('chkEye').checked,
      trend: document.getElementById('chkTrend').checked,
      sessions: document.getElementById('chkSessions').checked,
    };
    const resp = await fetch(BACKEND_URL + '/export/pdf', {
      method: 'POST',
      headers: authHeaders({'Content-Type':'application/json'}),
      body: JSON.stringify({
        sections,
        from: exportFromInput.value,
        to: exportToInput.value,
        patient_name: currentUser ? currentUser.name : 'BackTrack User',
      }),
    });
    if(!resp.ok){
      let msg = 'Failed to generate report.';
      try{ const err = await resp.json(); if(err.error) msg = err.error; } catch(e){}
      throw new Error(msg);
    }
    const blob = await resp.blob();
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = 'backtrack-report.pdf';
    document.body.appendChild(a);
    a.click();
    a.remove();
    URL.revokeObjectURL(url);
    pdfBtn.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg> Report downloaded';
  } catch(err){
    pdfBtn.innerHTML = original;
    showToast('Export failed', err.message || "Couldn't reach the BackTrack backend.");
  } finally {
    setTimeout(() => { pdfBtn.innerHTML = original; refreshPdfBtn(); }, 2200);
  }
});

/* ============ SETTINGS: BACKEND-BACKED THRESHOLD SLIDERS ============ */
const SETTINGS_FIELDS = [
  { key:'neck_thresh',    slider:'sliderNeckThresh',    label:'valNeckThresh',    display:v=>Math.round(v)+' degrees' },
  { key:'torso_thresh',   slider:'sliderTorsoThresh',   label:'valTorsoThresh',   display:v=>Math.round(v)+' degrees' },
  { key:'pitch_thresh',   slider:'sliderPitchThresh',   label:'valPitchThresh',   display:v=>Math.round(v)+' degrees' },
  { key:'roll_thresh',    slider:'sliderRollThresh',    label:'valRollThresh',    display:v=>Math.round(v)+' degrees' },
  { key:'shrug_ratio',    slider:'sliderShrugRatio',    label:'valShrugRatio',
    toUI:v=>Math.round(v*100), fromUI:v=>v/100, display:v=>Math.round(v*100)+'%' },
  { key:'shrug_dur',      slider:'sliderShrugDur',      label:'valShrugDur',      display:v=>Math.round(v)+' seconds' },
  { key:'side_dur',       slider:'sliderSideDur',       label:'valSideDur',       display:v=>Math.round(v)+' seconds' },
  { key:'ear_thresh',     slider:'sliderEarThresh',     label:'valEarThresh',
    toUI:v=>Math.round(v*100), fromUI:v=>v/100, display:v=>(v).toFixed(2) },
  { key:'blink_thresh',   slider:'sliderBlinkThresh',   label:'valBlinkThresh',
    toUI:v=>Math.round(v*100), fromUI:v=>v/100, display:v=>(v).toFixed(2) },
  { key:'low_blink_rate', slider:'sliderLowBlinkRate',  label:'valLowBlinkRate',  display:v=>Math.round(v)+' per minute' },
  { key:'min_blink_win',  slider:'sliderMinBlinkWin',   label:'valMinBlinkWin',   display:v=>Math.round(v)+' seconds' },
  { key:'ear_dur',        slider:'sliderEarDur',        label:'valEarDur',        display:v=>Math.round(v)+' seconds' },
  { key:'blink_dur',      slider:'sliderBlinkDur',      label:'valBlinkDur',      display:v=>Math.round(v)+' seconds' },
  { key:'head_dur',       slider:'sliderHeadDur',       label:'valHeadDur',       display:v=>Math.round(v)+' seconds' },
  { key:'cooldown',       slider:'sliderCooldown',      label:'valCooldown',      display:v=>Math.round(v)+' seconds' },
];

function applySettingsToUI(values){
  SETTINGS_FIELDS.forEach(f => {
    if(!(f.key in values)) return;
    const raw = values[f.key];
    const slider = document.getElementById(f.slider);
    const label = document.getElementById(f.label);
    const uiVal = f.toUI ? f.toUI(raw) : raw;
    slider.value = uiVal;
    label.textContent = f.display(raw);
  });
}

SETTINGS_FIELDS.forEach(f => {
  const slider = document.getElementById(f.slider);
  const label = document.getElementById(f.label);
  slider.addEventListener('input', () => {
    const raw = f.fromUI ? f.fromUI(Number(slider.value)) : Number(slider.value);
    label.textContent = f.display(raw);
  });
});

async function loadSettingsFromBackend(){
  const note = document.getElementById('settingsStatusNote');
  try{
    const resp = await fetch(BACKEND_URL + '/settings', { headers: authHeaders() });
    if(!resp.ok) throw new Error('bad status');
    const data = await resp.json();
    applySettingsToUI(data.settings || {});
    note.textContent = 'These thresholds are live — changes apply to detection as soon as you save.';
    note.classList.remove('hidden');
  } catch(err){
    note.textContent = "Couldn't reach the BackTrack backend at 127.0.0.1:5050 — showing default values until it's connected.";
    note.classList.remove('hidden');
  }
}

document.getElementById('saveSettingsBtn').addEventListener('click', async function(){
  const btn = this;
  const original = btn.textContent;
  const note = document.getElementById('settingsStatusNote');
  const payload = {};
  SETTINGS_FIELDS.forEach(f => {
    const raw = Number(document.getElementById(f.slider).value);
    payload[f.key] = f.fromUI ? f.fromUI(raw) : raw;
  });
  btn.textContent = 'Saving…';
  btn.disabled = true;
  try{
    const resp = await fetch(BACKEND_URL + '/settings', {
      method: 'POST',
      headers: authHeaders({'Content-Type':'application/json'}),
      body: JSON.stringify(payload),
    });
    const data = await resp.json();
    if(!resp.ok){
      throw new Error(Object.keys(data.errors || {}).length ? 'Some values were out of range and were not saved.' : 'Failed to save settings.');
    }
    applySettingsToUI(data.settings || {});
    btn.textContent = 'Preferences saved';
    note.textContent = 'Saved — the backend is now using these thresholds.';
    note.classList.remove('hidden');
  } catch(err){
    btn.textContent = original;
    showToast('Settings not saved', err.message || "Couldn't reach the BackTrack backend.");
  } finally {
    setTimeout(() => { btn.textContent = original; btn.disabled = false; }, 2000);
  }
});

document.getElementById('resetSettingsBtn').addEventListener('click', async function(){
  const note = document.getElementById('settingsStatusNote');
  try{
    const resp = await fetch(BACKEND_URL + '/settings/reset', { method: 'POST', headers: authHeaders() });
    if(!resp.ok) throw new Error('bad status');
    const data = await resp.json();
    applySettingsToUI(data.settings || {});
    note.textContent = 'Thresholds reset to defaults and saved.';
    note.classList.remove('hidden');
  } catch(err){
    showToast('Reset failed', "Couldn't reach the BackTrack backend.");
  }
});

/* ============ DELETE ALL DATA ============ */
document.getElementById('deleteDataBtn').addEventListener('click', async function(){
  const btn = this;
  const confirmed = window.confirm(
    "This permanently deletes every recorded session and resets your thresholds to defaults. This can't be undone. Continue?"
  );
  if(!confirmed) return;

  const original = btn.textContent;
  btn.disabled = true;
  btn.textContent = 'Deleting…';
  try{
    const resp = await fetch(BACKEND_URL + '/account/delete_data', { method: 'POST', headers: authHeaders() });
    const data = await resp.json();
    if(!resp.ok || !data.ok) throw new Error(data.error || 'Failed to delete data.');
    applySettingsToUI(data.settings || {});
    showToast('Data deleted', 'All sessions were removed and thresholds reset to defaults.');
    loadHistory();
  } catch(err){
    showToast('Deletion failed', err.message || "Couldn't reach the BackTrack backend.");
  } finally {
    btn.disabled = false;
    btn.textContent = original;
  }
});

/* ============ THEME OPTIONS ============ */
document.querySelectorAll('.theme-opt').forEach(opt => {
  opt.addEventListener('click', function(){
    document.querySelectorAll('.theme-opt').forEach(o => o.classList.remove('selected'));
    this.classList.add('selected');
  });
});
</script>

</body>
</html>
